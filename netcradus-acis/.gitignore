# Root level gitignore
.env
.env.local
.env.*.local
.vscode/
.idea/
*.log
*.err
out/
build/

# Kiro credentials — never commit. Real content is dropped in locally by the
# operator, referenced by docker-compose's kiro-gateway volume mount.
secrets/
