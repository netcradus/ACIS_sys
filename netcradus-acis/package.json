{
  "name": "netcradus-acis",
  "version": "1.0.0",
  "private": true,
  "description": "NETCRADUS ACIS - Autonomous Cyber Immune System",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview -- --host 0.0.0.0 --port 8080"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
